# 05. 確認側邊音量鍵是否正常

**狀態**：待處理 To-do
**認領視窗**：（尚未認領）

## 目標

確認側邊音量鍵（實體鍵）長期使用下是否穩定正常。

## 背景

之前已觀察到「以前這顆鍵不能用，後來某次系統更新後突然又能用了」，原因未查證。這次要做的是**驗證目前是否穩定正常**，如果又發現異常，順便回頭查是哪次更新造成的（可用 `dnf history list`、`journalctl -k -b` 跨開機比對）。

## 進度與發現

（尚無記錄）
