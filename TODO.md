# 待處理項目 / 目標清單

這份清單給多個平行執行的 Claude Code 視窗協作用。每個視窗認領一個項目、獨立處理，完成後回填這份文件並推送。

## 共同背景知識（每個視窗開始前都該先讀）

- **機型**：ASUS ZenBook UX370UAR（可翻轉觸控筆電，360° 轉軸）
- **系統**：Fedora 44，GNOME Shell 50.2
- **電源模式對應**：GNOME 的省電/平衡/效能三檔，實際是靠 `tuned-ppd`（不是 `power-profiles-daemon`）映射到 tuned profile：`power-saver→powersave`、`balanced→balanced(-battery)`、`performance→throughput-performance`。細節見 [issues/shift-key-drop.md](issues/shift-key-drop.md) 診斷過程。
- **已知但尚未深入診斷的舊觀察**：之前測試 Shift 掉鍵問題時，曾發現「接電源或不接電源、只要電源模式是省電，重開機到 GNOME 桌面時，輸入法中英切換圖示不會顯示，也點不到、Shift 也切不了」——這跟本清單的**項目1**是同一個現象，當時判斷可能跟掉鍵是不同成因，此清單的項目1負責window請從這裡接手深入查。
- **側邊音量鍵曾經失效過又自己恢復**：懷疑跟某次系統更新有關，細節可能跟**項目5**有關，尚未深究原因，只是現象曾被記錄。
- **同款硬體參考筆記**（不同發行版/桌面環境,僅供參考）：[shahmir-k/ASUS-ZenBook-Flip-S-UX370UAR](https://github.com/shahmir-k/ASUS-ZenBook-Flip-S-UX370UAR)（Linux Mint/Cinnamon）。裡面明確提到指紋辨識 Goodix GXFP3200 (SPI) **在 Linux 上不支援**，跟**項目6**直接相關；也提到 Hinge Sensor（HID-SENSOR-INT-020b，用來偵測翻轉角度做平板模式判斷）跟 accelerometer（HID-SENSOR-200073），跟**項目2b**直接相關。
- **repo 既有慣例**：`issues/` 放已經診斷、修復過的問題（症狀、根因、修法、風險）；`patches/` 放實際可套用的 patch 檔或完整備份檔。這份 `TODO.md` 是新增的第三類：**還沒開始或還在做的事**，做完後,如果衍生出診斷細節或 patch,請比照 `issues/`、`patches/` 的既有格式，另外開檔案，並在下面對應項目的「進度與發現」欄位連過去。
- **風險等級**：項目4（磁碟分割調整）具有破壞性，資料遺失風險高，處理前必須先完成備份要求，見該項目說明。

## 多視窗協作規則

1. 開始前先 `git pull`，確認拿到的是最新版本，避免跟其他視窗的認領/更新衝突。
2. 選一個「狀態」還是 `未開始` 的項目，把狀態改成 `進行中`，並在「認領視窗」欄位填上任意可辨識的標記（例如日期+項目編號，如 `2026-07-10 / item3`），**先 commit 這個認領動作再開始做事**，降低跟其他視窗撞題目的機率。
3. 處理過程中衍生的診斷紀錄、patch、腳本，比照既有的 `issues/`、`patches/` 慣例存檔。
4. 完成後（或卡住需要討論時），回來更新該項目：
   - 狀態改成 `已完成` / `卡住待討論` / `暫緩`
   - 在「進度與發現」欄位寫清楚做了什麼、發現什麼、還有什麼要注意，並連結新增的檔案
5. `git add` + `commit`（commit message 標明項目編號）+ `push`。
6. 如果 push 時發現別的視窗也改了這份 `TODO.md` 導致衝突，正常解衝突即可，不要整份用自己的版本覆蓋過去。

**狀態選項**：`未開始` / `進行中` / `已完成` / `卡住待討論` / `暫緩`

---

## 1. 省電模式下開機，輸入法圖示消失、無法切換中英文

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：不管接不接外部電源，只要電源模式是省電，開機後輸入法中英切換圖示都能正常顯示、Shift（或設定的切換鍵）能正常切換中英文。

**背景**：見上方共同背景知識。這是舊觀察到、尚未深入診斷的現象，跟 [issues/shift-key-drop.md](issues/shift-key-drop.md) 的掉鍵問題可能有共同的電源模式關聯，但機制可能不同（一個是「圖示完全消失、開機就壞」，一個是「單擊 Shift 偶爾沒反應」），需要重新從頭診斷，不要假設是同一個根因。

**進度與發現**：
（尚無記錄）

---

## 2. 觸控/翻轉相關功能（虛擬鍵盤、平板模式偵測與行為）

這一項底下四個子項彼此有關聯（都圍繞觸控/翻轉模式使用體驗），可以由同一個視窗依序處理，也可以拆給不同視窗，拆的話請在各子項「認領視窗」分開標記。

### 2a. Chrome / PWA 觸控輸入無法自動彈出虛擬鍵盤 → 手動按鈕呼叫/關閉

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：在 GNOME 頂部「wifi/藍牙/背光/音量」那個系統操作按鈕區（quick settings）新增一顆按鈕，按一下叫出螢幕虛擬鍵盤，再按一下收起。

**背景**：Chrome 瀏覽器與其 PWA app，觸控點擊文字輸入框時不會像原生 GTK app 一樣自動彈出虛擬鍵盤，這是已知現象（GNOME 的虛擬鍵盤彈出邏輯多半靠 GTK/IBus 的 focus 事件，Chromium 的 web 內容不會觸發同一套機制）。與其修 Chrome 本身，方向改成手動開關按鈕比較務實。

**進度與發現**：
（尚無記錄）

### 2b. 研究 Fedora 能否讀到翻轉鉸鏈角度感測器訊號

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：確認 Fedora 系統層級能不能讀到 UX370UAR 翻轉鉸鏈角度的感測器數值（不只是「有沒有進入平板模式」這個布林值，而是能不能拿到接近實際角度或至少能分辨「約210°~330°帳篷模式」與「360°平板模式」兩種區間）。

**背景**：同款硬體參考筆記提到 `HID-SENSOR-INT-020b`（Hinge Sensor，回報 lid angle）與 `HID-SENSOR-200073`（accelerometer，用於自動旋轉）都透過 Intel Sensor Hub 呈現。GNOME/systemd 這類角度資訊通常經由 `iio-sensor-proxy` 這個 daemon 暴露出來，可以先確認這個服務有沒有跑、有沦有抓到對應 sensor。翻轉超過180°時，ASUS 韌體/GNOME 本身可能已經有「帳篷模式=平板模式」的判斷（使用者觀察是這樣），要釐清這個判斷目前是在哪一層做的（韌體、kernel driver、iio-sensor-proxy、還是 GNOME Shell 自己）。

**進度與發現**：
（尚無記錄）

### 2c. 新增手動按鈕：一鍵進入/退出平板模式

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：不論 2b 有沒有成功抓到角度感測器訊號，都要在同一個系統操作按鈕區新增一顆按鈕，按一下強制進入 GNOME 平板模式，再按一下退出。

**背景**：這是 2b 的保底方案——就算感測器自動偵測搞不定，至少讓使用者能手動觸發同樣的平板模式狀態。

**進度與發現**：
（尚無記錄）

### 2d. 平板模式下的周邊行為：自動關觸控板 + 自動旋轉開關按鈕

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：
- 進入平板模式時，自動停用觸控板（不管是靠 2b 自動偵測進入、還是靠 2c 手動按鈕進入，都要生效）
- 系統操作按鈕區要新增一顆「是否自動旋轉螢幕」的開關按鈕，方便平板模式下切換
- 鍵盤不用特別處理——已觀察到 ASUS 韌體/硬體本身在翻轉到一定角度後會自動斷電關閉實體鍵盤，不需要另外寫邏輯

**背景**：這項依賴 2b 或 2c 至少一個先完成（要有「現在是不是平板模式」這個狀態可以掛觸發條件），建議排在 2b/2c 之後處理。

**進度與發現**：
（尚無記錄）

---

## 3. Claude Code 在終端機完成工作、或等待使用者回覆選項時，跳出系統通知

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：終端機裡的 Claude Code 工作做完、或是卡在等待使用者選擇（例如跳出 1/2/3 選項等回覆）時，要能跳出提示音 + GNOME 系統內建的通知小視窗。

**背景**：這比較像是 Claude Code 本身的設定/hook 問題，不是硬體或 Fedora 系統問題。Claude Code 有 hook 機制可以在特定事件（例如 Stop、等待輸入）時執行自訂指令，理論上可以掛一個會呼叫 `notify-send`（GNOME 通知）+ 播放提示音的指令上去。建議這個項目直接去看 Claude Code 官方的 hooks 文件/設定方式（`settings.json`），不用往 Fedora 系統本身找答案。

**進度與發現**：
（尚無記錄）

---

## 4. 1TB SSD 磁碟空間調整（雙系統）

**狀態**：未開始
**認領視窗**：（尚未認領）

**⚠️ 這項具有破壞性、資料遺失風險高，所有子項開始前必須先完成 4a 的備份要求，任何一個視窗處理這項都要再次跟使用者確認已經備份完成，才能繼續往下做。**

子項需要**依序**處理（b→c→d→e→f 有先後依賴關係），不建議拆給多個視窗同時做，避免互相干擾同一顆硬碟的分割表。

### 4a. 施工前備份提醒

**狀態**：未開始
**認領視窗**：（尚未認領）
**目標**：任何實際變動磁碟分割前，都要先明確提醒使用者做好 `.tib`（Acronis/Macrium 這類磁碟映像備份）備份，並在使用者確認「已備份」之前，不執行任何有破壞性的分割區操作指令。
**進度與發現**：
（尚無記錄）

### 4b. 縮小 data 槽（原本 600+ GB，實際用不到 100GB）到 500GB

**狀態**：未開始
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

### 4c. Fedora 自己的空間調整到 100GB

**狀態**：未開始
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

### 4d. 其他分割區調整成符合 UEFI 標準規範

**狀態**：未開始
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

### 4e. 確認雙系統開機是否正常，異常要修復到能開機為止

**狀態**：未開始
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

### 4f. 開啟 Fedora 的休眠（hibernate）功能

**狀態**：未開始
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

### 4g. 4a~4f 都完成後，再討論要不要挪一塊空間裝 Chrome OS Flex 或 Android-x86

**狀態**：未開始（依賴 4a~4f 全部完成才開始討論）
**認領視窗**：（尚未認領）
**進度與發現**：
（尚無記錄）

---

## 5. 確認側邊音量鍵是否正常

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：確認側邊音量鍵（實體鍵）長期使用下是否穩定正常。

**背景**：之前已觀察到「以前這顆鍵不能用，後來某次系統更新後突然又能用了」，原因未查證。這次要做的是**驗證目前是否穩定正常**，如果又發現異常，順便回頭查是哪次更新造成的（可用 `dnf history list`、`journalctl -k -b` 跨開機比對）。

**進度與發現**：
（尚無記錄）

---

## 6. 側邊指紋辨識 Goodix GXFP3200 (SPI) 尚無官方 Linux 驅動

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：追蹤 Goodix GXFP3200 (SPI) 指紋辨識模組是否有官方或社群（例如 `libfprint`、`fwupd` 社群、開發板/reverse-engineering 專案）釋出可用的 Linux 驅動或支援進度。

**背景**：同款硬體參考筆記（見共同背景知識）明確寫「Fingerprint: Goodix GXFP3200 (SPI) — not supported on Linux」。這項短期內大機率無解，性質比較像「定期追蹤官方/社群進度」，不是能一次做完的任務。

**進度與發現**：
（尚無記錄）

---

## 7. 觸控板雙指捲動（上下/左右）速度太快，想調慢 30%

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：觸控板雙指上下、左右捲動的畫面移動速度，調整為目前速度的約 70%（也就是慢 30%）。

**背景**：GNOME/libinput 的捲動速度可能可以透過 `gsettings`（`org.gnome.desktop.peripherals.touchpad`）或 libinput 設定檔調整，實際能不能做到精確的「-30%」還是只能選幾個預設檔位，需要實測確認。

**進度與發現**：
（尚無記錄）

---

## 8. 其他待補項目

之後想到新的待處理項目，比照上面任一項目的格式（狀態 / 認領視窗 / 目標 / 背景 / 進度與發現）新增在這個項目下面即可，不用等我另外整理格式。目前已經有兩項補進來，見項目 9、10。

---

## 9. 重開機/關機時 Chrome 沒等到正常關閉，下次開啟顯示「上次意外結束」

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：按下 GNOME 的重開機或關機按鈕時，如果 Chrome 還開著，系統要先給 Chrome 足夠的關閉等待時間（至少 5 秒），確保 Chrome 能正常寫入結束狀態，避免下次啟動時顯示「之前當機、意外結束」的提示。

**背景**：Chrome 是否顯示「上次未正常關閉」，是靠自己 Preferences 設定檔裡的 `exit_type` 欄位判斷——只有收到訊號後跑完自己的正常關閉流程，才會把這個值寫成 `Normal`；如果進程是被直接砍掉（來不及處理），就會留在 `Crashed`/`Abnormal`，下次啟動就跳出提示。GNOME/systemd 在 logout 或 shutdown 時，理論上會對使用者的應用程式送出終止訊號並等待一段時間才強制結束，但目前看起來等待時間不夠讓 Chrome 跑完。需要研究的方向：
- `systemd-logind`／`systemd` 在 session 結束、關機流程中，對一般使用者應用程式的等待時間設定在哪裡（例如 `DefaultTimeoutStopSec`、inhibitor lock 機制）
- 或者是不是要專門針對 Chrome 這類應用寫一個 shutdown/logout hook script，主動送出正常關閉訊號並等待，確保跑完再讓系統繼續關機流程
- 不確定 GNOME Shell 本身的 session 管理跟 systemd 的關機流程分別扮演什麼角色，這點也要查清楚再下手，不要一開始就假設是哪一層的問題

**進度與發現**：
（尚無記錄）

---

## 10. 讓 Chrome 的 LINE 擴充套件像 PWA 一樣獨立顯示在工作列

**狀態**：未開始
**認領視窗**：（尚未認領）

**目標**：讓 Chrome 上的 LINE 擴充套件，能像獨立的 PWA 應用程式一樣，在 GNOME 工作列/應用程式清單裡顯示成一個獨立圖示、獨立視窗，而不是依附在 Chrome 主視窗裡或只是工具列上的一顆小圖示。使用者提到 Chrome OS Flex 上可以做到這件事，想在 Fedora 上比照。

**背景**：Chrome/Chromium 支援把網頁「安裝為應用程式」（PWA，或舊式 Chrome App），安裝後會產生獨立的 `.desktop` 捷徑，讓 GNOME 把它當成一般應用程式對待、有自己的圖示與工作列項目，背後原理通常是用 `chrome --app=<url>` 這種模式獨立開視窗。要研究的重點：
- 使用者要的是「LINE 的**擴充套件**」而不是網頁版 LINE——擴充套件（extension）跟 PWA / Chrome App 是不同機制，需要先確認擴充套件本身有沒有辦法比照 PWA 包裝成獨立視窗/圖示，還是這條路根本行不通
- 如果擴充套件做不到，退一步的替代方案可能是改用「網頁版 LINE」（`line.me` 的網頁版）安裝成 PWA，取代擴充套件版本——但這是否等同、有沒有功能落差，需要先跟使用者確認是否可接受，不要自己決定切換方案
- Chrome OS Flex 能做到，代表底層 Chromium 本身具備這個能力，重點是怎麼在標準 Fedora + GNOME 環境下重現同樣效果（可能跟 Chrome OS 特有的 shelf/app list 整合有關，不一定能 1:1 搬過來，需要先查證）

**進度與發現**：
（尚無記錄）
